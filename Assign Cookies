class Solution:
    def findContentChildren(self, g: List[int], s: List[int]) -> int:
        g.sort()
        s.sort()
        left=0
        right=0
        while(left<len(g) and right<len(s)):
            if(s[right]>=g[left]):
                left+=1
                right+=1
            elif(s[right]<g[left]):
                right+=1
        return left

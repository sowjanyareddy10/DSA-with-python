from collections import Counter
class Solution:
    def frequencySort(self, s: str) -> str:
        d=Counter(s)#dict created
        u_d=sorted(d.items(),key = lambda x:-x[1])
        result =""
        for i,j in u_d:
            result+=i*j
        return result    

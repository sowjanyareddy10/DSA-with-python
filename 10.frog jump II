class Solution:
    def maxJump(self, stones: List[int]) -> int:#min steps calculate
        if len(stones)<=2:
            return stones[-1] #need to reach last
        ans=0
        for i in range(2,len(stones)):
            ans=max(ans,stones[i]-stones[i-2])#skip middle value
        return ans

              
        
